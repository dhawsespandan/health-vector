# Base URL for the backend API (Render service URL + /api in production)
# In Replit dev this is left blank so Vite proxies /api to the local server.
VITE_API_URL=https://your-render-service.onrender.com/api
